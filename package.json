{
  "name": "athanor",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'Run build from within a workspace extension directory'",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'Add a linter when blocks are in place'"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.37",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "crann": "^2.1.0",
    "typescript": "^5.9.3"
  }
}
